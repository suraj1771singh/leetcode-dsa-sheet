class Solution {
    int n, m;

    public long maxPoints(int[][] points) {
        n = points.length;
        m = points[0].length;
    }

    long f(int r, int c, int[][] points) {
        if (r < 0)
            return 0;
        dp[r][c] Math.max(dp[][], c)

    }
}
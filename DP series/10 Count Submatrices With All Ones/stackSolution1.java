import java.util.*;

//similar to find maximal number of rectangles

class Solution {
    public int numSubmat(int[][] mat) {
        int res = 0;
        Stack<Integer> st = new Stack<>();
        int n = mat.length, m = mat[0].length;
        return res;
    }
}